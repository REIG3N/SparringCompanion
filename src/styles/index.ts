// New exports
export * from './atomic';
export * from './components';
export * from './screens';

// Backwards-compatibility exports for existing code
// These are sourced from legacy JS global styles to avoid breaking changes
export { COLORS, OPACITY, SPACING, RADIUS, TEXT_STYLES, globalStyles } from '../../utils/globalStyles';


